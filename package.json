{
  "name": "parachuters",
  "version": "0.0.1",
  "description": "parachuters game",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "start": "http-server -c-1 --cors"
  },
  "author": "Michael Dombrovskis",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
