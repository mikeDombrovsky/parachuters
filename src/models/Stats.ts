export class Stats {
  score: number;
  lives: number;

  constructor() {
    this.score = 0;
    this.lives = 3;
  }

  // update() {
  //   if (this.lives <= 0) {
  //     // handle game over
  //   }
  // }
}
